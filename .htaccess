# Error handling
ErrorDocument 403 /404.php
ErrorDocument 404 /404.php
ErrorDocument 500 /404.php

<IfModule mod_rewrite.c>
  RewriteEngine On
  Options +FollowSymlinks +MultiViews -Indexes

  # Prevent access to sensitive folders
  RedirectMatch 404 /.login/?$
  RedirectMatch 404 /src/?

  # Mimic original directory URLs
  # TODO: Enable these
  # RewriteRule ^films/view/([0-9]*) /film.php?film_id=$1 [QSA,NC,L]
  # RewriteRule ^films/view/([0-9]*)/$ /film.php?film_id=$1 [QSA,NC,L]

  # Define new clean URLs
  # TODO: Enable these
  # RewriteRule ^film/(\d*) /film.php?film_id=$1 [QSA,NC,L]
  # RewriteRule ^film/(\d*)/$ /film.php?film_id=$1 [QSA,NC,L]
</IfModule>

# TODO: Add caching, compressing, expires
